*.iml
.gradle/
local.properties
.idea/
build/
app/build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
*.keystore
*.jks

# 构建产物目录。APK 已由 *.apk 覆盖，这里连带挡掉脚本在 dist/ 下可能产生的中间文件
# （2026-09-19 踩坑：verify_launch.py 曾在 dist/ 下解包 dex，8 MB 的 classes.dex
#   被 *.dex 未覆盖的空子提交进了仓库）
dist/

# 个人账单样本（真实数据，不进版本库）
samples/

# 工作区元数据：代理的项目记忆（含真实账单的聚合数字）与一次性探针脚本。
# 属于本地工作产物，不是源码，没有共享价值。2026-09-20 前误入库 12 个文件，已 git rm --cached 移出。
.workbuddy/

# 无头构建的临时输出日志（cmd 重定向产物，编码还是 UTF-16，无保留价值）
build*.log
