# 离线记账 App 总体设计方案（OfflineLedger）

> 版本：v1.2（方案定稿候选，待 §11 剩余开放问题拍板后进入实现）
> 日期：2026-09-15
> 前置文档：可行性评估与路径对比（2026-09-10 会话）、账单导出路径事实核查（2026-09-11 会话）、GitHub 开源方案调研（2026-09-15，`docs/开源方案调研.md`）、第一性原理审计（2026-09-15 会话，结论已全部吸收）
>
> **v1.2 变更记录**：吸收第一性原理审计全部 7 条结论——① 新增「合并回填」设计，修复双记账缺陷（§4.3，核心修订）；② 新增 M0 编译里程碑（§8）；③ 归类准确率量化验收（§9）；④ 首页「最近导入」状态条（§3.2/§8）；⑤ 备份导出从 P4 提前至 P3（§8）；⑥ 支付宝「交易分类」列作初始分类种子（§4.4）；⑦ P3/P4 冻结纪律（§8）。用户同时确认接受两大天花板事实（§1.3），不再视为待决问题。
>
> **v1.1 变更记录**：应用户要求放宽「绝对离线」约束——真实诉求是「数据不被偷偷上传」，而非物理断网。修订为**「数据永不出户 + 可选受控联网」**：联网仅用于解析规则的单向下载（手动触发、白名单域名、零上传路径），并保留 `offline` 构建版作为物理断网的兜底选项。其余架构不变。

---

## 0. 方案摘要

| 项 | 结论 |
|---|---|
| 产品形态 | 纯本地 Android 记账 App，侧载自用，包名 `com.ledger.offline` |
| 数据获取 | **xlsx 导入为主力与地基，通知监听为日常自动补充，无障碍读屏为可选开关（默认关）** |
| 自动分类 | 三级规则链：用户修正记忆 → 关键词规则（外置 JSON）→ 兜底「未分类」。**不引入 AI 模型**；支付宝「交易分类」列作初始种子（v1.2） |
| 去重与合并回填（v1.2 新增） | xlsx 导入对既有通知记录**先合并、后判重**：按单号/宽松指纹匹配后回填商户名、交易单号、官方分类——既杜绝双记账，又是归类准确率的主要来源（§4.3） |
| 联网边界（v1.1 修订） | **数据永不出户**。双构建版本：`offline` 版零 INTERNET（内核级断网）；`plus` 版声明 INTERNET，但唯一用途是**规则单向下载**（手动触发 + 白名单域名 + 无任何上传代码路径） |
| 规则维护（v1.1 新增） | 规则包托管在 GitHub/Gitee 仓库，`plus` 版在 App 内检查更新、哈希校验、沙盒验证通过后热替换；借鉴 AutoAccounting 的规则更新模式但砍掉其自动后台联网 |
| 体积预算 | APK ≤ 2.5 MB（plus 版 +50~100 KB 网络模块）；三年数据 ≤ 8 MB |
| 数据安全 | 字段级 AES-GCM 加密（密钥由 Android Keystore 托管），索引字段存 HMAC 摘要，不存明文 |

与最初设想的最大差异：**xlsx 导入从「兜底」升为主力**。原因有二——它是唯一 100% 完整且带收款方/收支方向的数据源；且用户已实测确认微信、支付宝导出物均为 xlsx，路径确定可走通。通知监听保留，承担「日常免操作」的实时性价值。

---

## 1. 需求与边界

### 1.1 做什么

1. 自动获取微信、支付宝的支付记录（金额、收款方、时间、收支方向）
2. 按预设支出类型自动归类，用户可修正，修正结果被永久记忆
3. 流水列表 + 月度汇总 + 分类统计
4. 所有数据本地加密存储，可导出备份
5. 隐私边界（v1.1 修订）：**数据永不出户**——联网（可选开启）仅限规则单向下载，代码层面无任何上传路径、无设备标识、无遥测；`offline` 构建版保留物理断网保证

### 1.2 不做什么（明确的非目标）

| 非目标 | 原因 |
|---|---|
| 上架应用商店 | 自用侧载；商店对通知/无障碍权限审核极严 |
| AI 分类模型 | TFLite 模型 5~30 MB，体积是 App 全部预算的十倍以上，准确率反不如规则+记忆 |
| 云端同步 / 多设备 | **数据出户即违背核心诉求**，与联网放宽无关，仍然禁止 |
| 云端备份用户数据 | 同上；备份只走本地导出文件 |
| 银行卡短信解析 | 覆盖不了微信/支付宝，且权限敏感 |
| 截屏 OCR | 耗电、隐私风险大、准确率差 |
| 预算管理 / 多账本 / 报销 | 超出「记账 + 归类」核心闭环，后续再议 |
| 后台自动联网更新 | 联网必须用户手动触发且界面可见，杜绝任何静默网络行为 |

### 1.3 必须诚实接受的天花板（不是 bug，不要试图"修复"）

- 微信/支付宝的通知**经常不带收款方**，此类记录只能标「未识别商户」，靠长按修正一次后记忆
- 通知监听覆盖率约 70~80%（免密支付、自动续费、银行卡直刷等不产生通知）
- 账目完整性 100% 依赖每月/每季度一次 xlsx 导入补齐，**申请导出这一步本身需要联网**（在 App 之外，由用户手动完成）
- 国产 ROM 会杀后台，需一次性白名单配置

**以上天花板用户已于 2026-09-15 确认接受，作为需求基线的一部分，不再视为待决问题**：①「自动」的上限由微信/支付宝的通知策略决定，是外部约束，非代码可破；②「准确」的兑现路径 = 合并回填（§4.3）+ 修正记忆（§4.4）逐步收敛，而非开箱即 100%。

---

## 2. 关键事实基础

### 2.1 已验证（有官方文档或用户实测支撑）

| 事实 | 来源 |
|---|---|
| 微信、支付宝导出的账单均为 **.xlsx** | 用户实测（2026-09-11） |
| 微信导出路径：我→服务→钱包→账单→···→下载账单→用于个人对账 | 微信官方文档 pay.weixin.qq.com |
| 微信有两种接收方式：服务号（免解压码）/ 邮箱（加密 zip + 6 位解压码） | 微信官方文档 |
| 微信单次最长导出 1 年，24 小时内下发 | 微信官方文档 |
| 账单存储在腾讯/阿里云端服务器，手机本地无现成文件 | 微信官方文档 |
| 支付宝导出路径：我的→账单→···→开具交易流水证明→用于个人对账 | 多来源一致 |

### 2.2 待验证（影响实现细节，不阻塞方案）

| 待验证项 | 影响 |
|---|---|
| 微信「服务号接收」的 xlsx 能否保存到手机文件夹 | 决定历史回填能否纯手机闭环，还是要过一遍电脑 |
| 用户手机上支付通知是否带商户名 | 决定自动分类的实际上限 |
| 两家 xlsx 的真实表头与前置说明行行数 | 解析规则需用真实文件校准（表头名定位，不依赖列号） |

---

## 3. 总体架构

### 3.1 数据获取策略（三路）

```
                    ┌─────────────────────────────┐
                    │  微信/支付宝 云端（需联网申请） │   ← 联网环节①，
                    └──────────────┬──────────────┘     发生在 App 之外
                                   │ xlsx 文件
   ═══════════ 手机本地边界 ═══════════
                                   │
   ┌───────────┐  ┌───────────┐  ┌─┴─────────┐
   │ 通知监听    │  │ 无障碍读屏  │  │ xlsx 导入   │
   │（自动·主力）│  │（可选·默认关）│  │（手动·地基） │
   └─────┬─────┘  └─────┬─────┘  └─────┬─────┘
         └──────────────┼──────────────┘
                        ▼
        解析 → 去重 → 归类 → 字段级加密 → SQLite
                        ▲
   ┌────────────────────┴───────────┐
   │ 规则包（JSON，本地可热替换）        │ ← 联网环节②（可选·plus 版专有）：
   └────────────────────────────────┘   手动触发、白名单域名、仅下载规则
```

**联网边界总述（v1.1）**：整个系统只有两个联网环节，①申请账单导出（在微信/支付宝 App 内，用户手动），②规则包下载（在本 App 内，用户手动触发，`offline` 版没有此环节）。**两个环节都是单向获取，没有任何用户数据外发路径。**

三路的定位：

| 通道 | 角色 | 覆盖率 | 用户负担 | 失败模式 |
|---|---|---|---|---|
| **xlsx 导入** | 地基 + 对账基准 | 100%（官方全量） | 每月/每季度手动一次 | 无（解析失败则明示丢弃并计数） |
| **通知监听** | 日常自动 | 70~80% | 一次授权 + 白名单 | 漏通知（由 xlsx 补齐）、无商户名（由记忆补齐） |
| **无障碍读屏** | 可选补漏 | 不确定 | 高（耗电、微信可能风控） | 默认关闭，出问题随时关 |

### 3.2 分层结构（沿用已生成骨架，粗体为本方案修订点）

```
capture/   NotificationCaptureService · ScreenReadService（默认关）· BootReceiver
           BillImporter（原 CsvIo 改造：xlsx 入口 + 来源识别）
parse/     TransactionParser（通知文本）· RuleStore（assets→filesDir 热替换）
           ★ XlsxReader（新增：系统 zip + XmlPull 流式读 xlsx）
classify/  MerchantNormalizer · Classifier（记忆 > 关键词长度决胜 > 兜底）
data/      LedgerDb · TransactionDao（v1.2：先合并回填后判重，四级匹配，见 §4.3）
crypto/    FieldCipher（Keystore AES-GCM + HMAC 索引摘要，密文以 BLOB 存储）
core/      ServiceLocator（手工 DI）
net/       ★ RuleUpdater（v1.1 新增，仅 plus 版编译入包：HttpURLConnection
           单向下载 + 域名白名单 + 哈希校验 + 沙盒验证）
ui/        MainActivity · TransactionAdapter · 月度/分类统计 · 规则更新入口
           ★ 首页「最近导入」状态条（v1.2：覆盖率可见性，防忘导入导致账目长期残缺）
```

### 3.3 技术选型约束（不可违背）

1. **零第三方运行时依赖**：仅 androidx.core-ktx / appcompat / recyclerview / constraintlayout。禁止 Room / Hilt / Material Components / **Retrofit / OkHttp** / 任何统计与崩溃 SDK——联网功能用系统自带 `HttpURLConnection` 实现（零体积、零依赖），**绝不为此引入网络库**
2. **联网约束（v1.1 修订）**：采用 **Gradle productFlavors 双版本**——`offline` 版不声明 INTERNET（保留物理断网保证）；`plus` 版声明 INTERNET 但全部网络代码收敛在 `RuleUpdater` 单类中，白名单域名硬编码。两版共享其余全部代码。`offline` 版每次构建后跑 `tools/verify_offline.sh`，`plus` 版跑 `tools/verify_network_safety.py`（检查网络调用点仅存在于 RuleUpdater）
3. **解析规则一律外置 JSON**（`filesDir/parser_rules.json` > `assets/parser_rules.json`），微信/支付宝改版后热替换，不重新出包
4. **不引入 Apache POI**：10 MB 级 JVM 库，Android 上既超体积又跑不动；xlsx 用系统自带 `java.util.zip` + XML 解析器实现
5. **字段级加密，不用 SQLCipher**：省 2~3 MB native .so
6. **解析宁缺毋滥**：金额解析不出、收支方向判不出 → 直接丢弃并计数，不猜默认值。错误的账目比缺失的账目更糟
7. **无任何上传路径（v1.1 新增）**：代码审查红线——不出现任何 POST/PUT 请求、不采集设备标识（Android ID / IMEI / 序列号）、不写任何遥测埋点。`verify_network_safety.py` 以静态扫描兜底

---

## 4. 核心设计

### 4.1 xlsx 解析层（本方案唯一全新模块）

xlsx = zip 容器 + 若干 XML。最小读取器只需三个部件：

| 部件 | 作用 | 实现 |
|---|---|---|
| 容器解包 | 读取 zip 内指定条目 | 系统自带 `java.util.zip.ZipInputStream` |
| 共享字符串表 | `xl/sharedStrings.xml` → 字符串数组 | 系统 XML 流式解析 |
| 工作表 | `xl/worksheets/sheet1.xml` → 行/单元格流 | 同上 |

实现要点：

- **流式解析**，不把整个 XML 载入内存（年度账单可达数万行）
- 单元格类型处理：`s`（共享字符串索引）、`inlineStr`、`n`（数字）；按 `r` 属性（如 `B5`）定位列，容忍空单元格
- **表头自动定位**：微信/支付宝的 xlsx 开头有若干行说明文字，真实表头在中间。扫描前 N 行，命中表头特征词（如「交易时间」「交易对方」「收/支」）即锁定表头行
- **来源识别**：按表头特征区分微信 vs 支付宝（两家列名不同），映射关系外置到 `parser_rules.json` 的 `importProfiles`
- **按表头名取列**，绝不按列号硬编码（对方改版调列序不会炸）

关键映射（字段名以真实文件校准）：

| 统一字段 | 微信 xlsx | 支付宝 xlsx |
|---|---|---|
| 时间 | 交易时间 | 交易创建时间 / 付款时间 |
| 商户 | 交易对方 | 交易对方 |
| 金额 | 金额(元) | 金额（元） |
| 收支方向 | 收/支 | 收/支 |
| 交易单号 | 交易单号 | 交易号 |
| 状态过滤 | 当前状态 | 交易状态 |
| 支付方式 | 支付方式 | （收/支 栏之外的账户信息） |

过滤规则：

- 仅导入「交易成功」状态的记录
- 丢弃「不计收支 / 中性交易」（零钱转入转出、信用卡还款等内部资金移动）
- 丢弃「已全额退款」；部分退款在备注体现，不单独建模
- 金额列含千分位逗号（`1,299.00`），解析前去逗号（此缺陷已在通知解析侧修复过，xlsx 侧沿用同一清洗函数）

### 4.2 通知解析层（已有，保留）

- `NotificationCaptureService`：包名早筛（仅微信/支付宝）+ 10 秒签名短路
- `TransactionParser`：金额正则 `[0-9][0-9,]*`（千分位已修复）、方向关键词表、`NOISE_MERCHANTS` 商户黑名单、`STOP_WORDS` 截断
- 解析不出金额或方向 → 丢弃（宁缺毋滥原则）
- 已用 `tools/verify_parser_rules.py` 验证 9/9（含 3 个负例）

### 4.3 去重与合并回填（v1.2 核心修订，原「去重（双路）」）

同一笔交易会同时被「通知」和「xlsx」捕获。v1.1 只做判重丢弃，存在审计确认的缺陷：**通知记录经常缺商户名（空或「未识别商户」），与 xlsx 记录的真实商户名指纹对不上 → 判重失败 → 同一笔交易双记账**。v1.2 改为「先合并、后判重」。

**xlsx 记录入库时的匹配顺序（逐条执行）：**

| 序 | 匹配规则 | 命中后动作 |
|---|---|---|
| 1 | 交易单号精确匹配（`txn_no`） | **合并回填**，不新插入 |
| 2 | 宽松指纹：`金额 + 方向 + ±3 分钟时间窗`（仅当既有记录来自通知且商户为空/未识别） | **合并回填** |
| 3 | 完整指纹：`金额 + 商户 + 方向 + ±3 分钟`（双方都有商户名） | 丢弃 xlsx 副本，保留原记录 |
| 4 | 全部未命中 | 作为新记录插入 |

反向场景（xlsx 先入库、通知后到）同样处理：通知记录命中既有单号/指纹 → 直接丢弃通知副本（通知是信息量少的一方，永远不覆盖 xlsx）。

**合并回填的内容**——xlsx 是唯一自带商户名的数据源，回填直接决定归类准确率：

| 回填字段 | 来源 | 作用 |
|---|---|---|
| 商户名 | xlsx「交易对方」 | 「未识别商户」记录获得可归类主体 |
| 交易单号 | xlsx 交易号 | 此后按单号精确判重，永不再重 |
| 官方分类 | 支付宝「交易分类」 | 分类种子（§4.4） |
| 对账标记 | — | 标记「已对账」，统计页可区分账目完整区段 |

实现约束：
- 指纹存 HMAC 摘要（`amount_hash` / `merchant_hash`），不解密也能查
- 回填只补空字段，**永不覆盖用户已修正的分类**（修正记忆优先级最高，不可被任何自动逻辑推翻）
- 该逻辑在 P0 实现，用单测构造「通知先入库 + xlsx 后导入」的重叠数据验证；P1 真机复验

### 4.4 分类引擎（三级，全部本地）

```
商户名归一化（去分店/城市后缀）→
  ① 用户修正记忆（merchant_override 表，优先级最高，永久生效）
  ② 关键词规则（classify_rules.json，按关键词长度决胜，避免「星巴克臻选」输给「星」）
  ③ 兜底「未分类」
```

- 预设分类：餐饮、交通、购物、居住、医疗、娱乐、转账、红包、教育、其他（可编辑）
- **修正记忆是准确率的核心来源**：用户长按改一次分类 → 写入 `merchant_override` → 同类交易永久自动命中。用得越久越准
- **支付宝「交易分类」列作初始分类种子（v1.2 修订）**：支付宝导出自带真实分类（餐饮/交通等），导入及合并回填时直接映射到预设类别，初始未分类率大幅下降。微信的「交易类型」（商户消费/转账/红包）**不是**支出类别，仅作辅助信号——两家区别对待，不混用
- 官方分类是种子不是终裁：优先级仍低于用户修正记忆与关键词规则命中（官方分类粒度粗，如「日用百货」vs 用户自定义的「母婴用品」）

### 4.5 存储与加密

| 设计 | 决策 |
|---|---|
| 数据库 | 系统自带 SQLite（零依赖） |
| 加密粒度 | 字段级 AES-GCM，密钥由 Android Keystore 托管（密钥不出安全硬件） |
| 加密字段 | 金额、商户名、备注 |
| 索引字段 | HMAC 摘要（`amount_hash` / `merchant_hash`），支持去重与查询，不解密 |
| 密文存储 | **二进制 BLOB**，禁止 Base64（否则多膨胀 33%） |
| 时间/分类/来源 | 明文（统计需要，敏感度低） |

### 4.6 规则热替换

- 加载优先级：`filesDir/parser_rules.json` > `assets/parser_rules.json`
- 修改固定流程：改 JSON → 跑 `python tools/verify_parser_rules.py` → 通过后再同步 Kotlin 与单测 → `./gradlew :app:testDebugUnitTest`

### 4.7 规则在线更新（v1.1 新增，仅 plus 版）

**动机**：v1.0 中规则热替换虽然不用重新出包，但更新规则仍需「电脑改 JSON → adb push / 文件管理器放入 filesDir」，摩擦大。微信/支付宝改版后解析失效的窗口期内，通知采集等于停摆。参考 AutoAccounting 的 RuleUpdateHelper / AutoRuleSubmit 模式（该项目的规则由社区经 GitHub Issue 提交、App 内检查更新），吸收其「规则与代码分离、云端维护」的思路，但**砍掉它的自动后台更新**——所有联网动作必须是用户手动触发、界面可见。

**规则包格式**（托管在 Git 仓库，GitHub 主源 + Gitee 镜像二选一）：

```
rules/
  manifest.json          # 版本号 + 各文件 SHA-256 + 兼容的 App 最低版本
  parser_rules.json      # 通知解析 + xlsx importProfiles
  classify_rules.json    # 关键词分类规则
  samples_positive.json  # 必须解析成功的样本（含 double-entry-generator
  samples_negative.json  # 与 seamless-bookkeeping 的真实样本）
```

**更新流水线（五道闸门，任何一道失败即放弃、保留原规则）**：

```
手动点「检查更新」
  → ① GET manifest.json（域名白名单校验，非白名单 host 直接拒绝）
  → ② 比对版本号，无新版本则结束
  → ③ 下载规则文件 → SHA-256 校验
  → ④ 沙盒验证：用内置校验器跑 samples_positive/negative，
       新规则必须全样本通过（宁缺毋滥原则同样适用于规则本身）
  → ⑤ 通过后原子替换 filesDir 规则；失败则保留原规则并明示原因
```

**网络面收窄的工程保证**：

| 手段 | 说明 |
|---|---|
| 唯一出口类 | 全部网络代码收敛在 `net/RuleUpdater.kt` 单文件（约 200 行），其余代码零网络调用，静态可审计 |
| 域名白名单硬编码 | `raw.githubusercontent.com` / `gitee.com`，host 不匹配即拒绝连接 |
| 只 GET 不 POST | 请求无参数、无设备标识、无 cookie；服务端无法从请求中获知任何用户信息 |
| 禁明文流量 | `network_security_config` 中 `cleartextTrafficPermitted="false"` |
| 手动触发 | 设置页显式按钮，无 WorkManager/定时任务/开机自动检查 |

**规则源内容来源**：初始规则直接吸收开源调研的成果——double-entry-generator 的两家列名映射与收支/交易类型枚举、seamless-bookkeeping 的 6 条真实通知样本与负向断言正则。日后微信/支付宝改版，改仓库里的 JSON 即可，App 内一键更新。

**offline 版兼容性**：`RuleUpdater` 类在 offline flavor 中不编译入包（sourceSet 隔离），设置页也不显示更新入口；offline 版更新规则仍走 v1.0 的本地热替换路径。

---

## 5. 隐私与安全（v1.1 修订）

| 措施 | 说明 |
|---|---|
| 数据永不出户 | 联网仅限规则下载（单向 GET）；代码无 POST、无设备标识、无遥测，静态扫描兜底 |
| offline 版物理断网 | 不声明 INTERNET，内核级无法创建 socket；担心任何风险时装这一版 |
| 构建后验证 | offline 版跑 `tools/verify_offline.sh`（合并后 Manifest 无 INTERNET）；plus 版跑 `tools/verify_network_safety.py`（网络调用点仅存在于 RuleUpdater + 权限清单核对 + 体积报告） |
| 字段级加密 | 敏感列 AES-GCM；Keystore 托管密钥 |
| 不导出组件 | 无 exported Activity/Provider；关 `usesCleartextTraffic`；`data_extraction_rules` 禁云备份 |
| 权限最小化 | offline 版 4 项 / plus 版 5 项（见 §7） |
| 诚实告知 | 通知使用权是**全量**的（系统不提供按 App 过滤），代码只处理两个包名，但权限覆盖全部通知。v1.0 靠零 INTERNET 对冲；v1.1 的对冲变为「plus 版无上传路径 + offline 版物理断网」，**担心通知内容泄露时改装 offline 版即可** |
| 规则包防投毒 | HTTPS + manifest 内 SHA-256 + 沙盒全样本验证，三重闸门后才替换生效 |

---

## 6. 体积与性能预算

| 项 | 预算 | 依据 |
|---|---|---|
| APK（offline 版） | ≤ 2.5 MB | androidx 四库 1.2~1.8 MB + Kotlin/代码 ~250 KB + 资源 ~100 KB |
| APK（plus 版） | ≤ 2.6 MB | 同上 + RuleUpdater ~200 行（HttpURLConnection 是系统 API，零体积） |
| APK（激进档） | 0.6~1.0 MB | 砍 appcompat 用纯平台 Activity（备选，见 §11 开放问题） |
| 单条记录 | 200~350 字节 | BLOB 密文 + HMAC 摘要 + 明文索引列 |
| 三年数据 | ≤ 8 MB | 按每年 3000 笔 |
| 内存 | 20~40 MB | 无图片缓存；网络栈仅在更新规则瞬间存在 |
| 耗电 | 通知监听≈0（系统事件驱动，不占通知栏）；无障碍明显（默认关）；规则更新仅在手动触发时联网数秒 |

---

## 7. 权限与安装

| 权限 | 类型 | 必需性 | 拒绝后果 |
|---|---|---|---|
| 通知使用权 | 特殊权限，手动开启 | 自动采集必需 | 退化为纯导入型 App |
| 无障碍服务 | 特殊权限，手动开启 | **可选，默认关** | 无影响 |
| POST_NOTIFICATIONS | Android 13+ 运行时权限 | 建议 | 收不到 App 自身提醒 |
| RECEIVE_BOOT_COMPLETED | 普通权限 | 必需 | 重启后需手动开 App |
| 文件读取 | **不需权限**（SAF 系统文件选择器） | 导入必需 | — |
| INTERNET | 普通权限 | **仅 plus 版声明**，且仅 RuleUpdater 使用 | offline 版不声明；plus 版无此权限则规则更新不可用，其余功能不受影响 |

安装要求：Android 8.0+ / arm64-v8a / 侧载（临时开未知来源，装完可关）/ 自备签名密钥（**丢失则无法覆盖升级**，需备份提醒）。**offline 与 plus 两版共用同一签名，可互相覆盖安装，数据不丢**——平时用 plus 版，任何疑虑期随时切回 offline 版。

---

## 8. 实施路线图

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| **M0 · 编译打通（v1.2 新增）** | Android Studio 编译既有骨架 + 现有单测 + verify 脚本全部跑通 | 零编译错误；3 个测试文件全绿；`verify_parser_rules.py` 9/9。**任何新代码动工前必须先过 M0** |
| **P0 · 导入闭环** | XlsxReader + BillImporter（含 §4.3 合并回填逻辑）+ 存储加密 + 流水列表/月度统计 + 首页「最近导入」状态条 | 真实 xlsx 导入成功、金额分毫不差；重叠时段二次导入零重复（单测覆盖回填路径）；导入后未分类占比 < 15% |
| **P1 · 自动采集** | 通知监听 + 双路去重融合 + 白名单引导页 | 真实支付 5 秒内自动入账；导入含同期账单后原「未识别商户」记录被自动回填；零重复 |
| **P2 · 越用越准** | 修正记忆闭环 + 分类统计 + 规则热替换实测 | 长按改一次分类后同类新记录自动命中；改 JSON 不重装即生效；月度手动修正次数环比递减 |
| **P3 · 数据安全 + 规则在线更新**（v1.2 重排） | 本地备份导出（SAF 写文件，**修正记忆随库导出**）+ plus flavor + RuleUpdater + 规则仓库初始规则包 | 导出文件可再导入，数据往返一致；规则更新全程 < 1 分钟，五道闸门任一失败保留原规则 |
| **P4 · 按需** | 无障碍可选开关、年度统计 | 手动开启后命中率与耗电实测可接受 |

**冻结纪律（v1.2 新增）**：P3/P4 冻结，直至 P0~P2 在真机上全部验收通过再解冻。个人项目最常见的死因不是技术失败，而是范围失控后弃坑——v1.1 引入规则仓库与双 flavor 后范围已膨胀，先拿到一个真机可用的 P2，再谈增强。

P0 排第一的原因：xlsx 是**确定性最高**的路径（格式已知、可用真实文件立即验证），先跑通它，即使通知采集后续遇到意外，App 已经是一个可用的纯导入型记账工具。

P3 的构成原因（v1.2 修订）：备份导出从 v1.1 的 P4 **提前**——账单本身可重新导出导入（丢失可恢复），但修正记忆是数月积累的资产，丢失后要从头重新训练，其保护优先级高于规则在线更新；两者互不阻塞 P0~P2，故并列一段。规则在线更新仍排在本地热替换（P2）稳定之后，避免规则格式频繁变动导致线上规则包反复返工。**P0~P2 全程不需要任何联网决策，offline 单版本即可推进。**

---

## 9. 测试与验收

| 层 | 手段 |
|---|---|
| 解析规则 | `tools/verify_parser_rules.py`（Python 复刻 Kotlin 判定逻辑，含负例断言）；xlsx 样例用 Python `zipfile`+`xml` 复刻同一套表头定位/清洗逻辑先行验证 |
| 单元测试 | XlsxReader（构造最小 xlsx 二进制）/ TransactionParser / Classifier / Importer / RuleUpdater（域名白名单、哈希校验、坏规则拒收），纯 JVM 不依赖 Android |
| 离线验证（offline 版） | `tools/verify_offline.sh`：合并后 Manifest 无 INTERNET + APK 体积报告 |
| 网络安全验证（plus 版，v1.1 新增） | `tools/verify_network_safety.py`：① 合并后 Manifest 权限清单仅比 offline 版多 INTERNET；② 反编译 dex 静态扫描，`openConnection`/`URLConnection`/`HttpClient` 等网络 API 的调用点必须全部位于 `RuleUpdater`；③ 扫描无 POST 语义调用、无 `ANDROID_ID`/`getDeviceId` 等标识采集 |
| 真机验收 | 真实 xlsx 导入零静默错误（丢弃必须计数并可见）；真实支付 5 秒自动入账；杀后台重启后服务自愈；plus 版规则更新五道闸门逐条故障注入（断网/坏哈希/坏规则各验一次） |
| 量化验收（v1.2 新增） | ① xlsx 导入后未分类占比 **< 15%**（用真实账单验收）；② 重叠导入构造测试**重复率 = 0**；③ 静默错误 = 0（所有丢弃必须计数且用户可见）；④ 月度手动修正次数**环比递减**（P2 后按月统计，检验修正记忆是否生效） |

---

## 10. 风险登记册

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| 微信/支付宝改 xlsx 表头 | 中 | 导入失败 | 按表头名定位 + 规则外置热替换 + plus 版在线更新规则 |
| 通知无商户名 | **高** | 自动分类降级 | 修正记忆 + 月度 xlsx 对账兜底 |
| 后台被 ROM 杀 | 高 | 漏通知 | 白名单引导 + 打开 App 时检测提示 + xlsx 补齐 |
| 签名密钥丢失 | 低 | 无法覆盖升级 | 生成时强制提示备份 + 数据导出功能 |
| 无障碍触发微信风控 | 中 | 账号风险 | 默认关闭，文档明示风险 |
| 通知使用权全量暴露 | 确定存在 | 理论隐私面 | plus 版无上传路径可审计；offline 版物理断网；随时可互切 |
| 通知与 xlsx 双路重复入账（v1.2 识别） | **高**（若无合并逻辑） | 账目错误，直接摧毁对账本的信任 | §4.3 先合并后判重 + 宽松指纹回填 + 单测构造重叠数据覆盖 |
| 用户忘记月度导入，账目长期残缺（v1.2 识别） | 中 | 覆盖率停在 70~80%，价值崩塌 | 首页「最近导入」状态条常驻提醒；超 40 天未导入显式告警 |
| 规则仓库被投毒/劫持（v1.1 新增） | 低 | 解析失效或错乱 | HTTPS + SHA-256 校验 + 沙盒全样本验证三重闸门，坏规则进不来；GitHub 私有仓库 + Gitee 镜像双源 |
| 引入联网后放松警惕、混入第三方 SDK（v1.1 新增） | 中 | 偷偷上传数据、体积膨胀 | `verify_network_safety.py` 每次构建强制跑；零第三方依赖红线不因联网放宽而动摇 |

---

## 11. 待你拍板的开放问题

1. **体积取舍**：保 appcompat（1.5~2.5 MB，开发省心）还是纯平台 Activity（0.6~1.0 MB，跨版本一致性自己兜）？— 建议保 appcompat，2 MB 已足够小
2. **微信服务号通道**：xlsx 能否从微信保存到手机文件夹？— 决定历史回填是纯手机闭环还是要过电脑
3. **你的支付通知带不带商户名**：翻 5 条最近通知即可确认 — 决定自动分类实际上限
4. ~~是否需要本地备份导出~~ → **已拍板（2026-09-15）：做，且从 P4 提前至 P3**
5. **规则仓库形态（v1.1 新增）**：a) 自建 GitHub 私有仓库（只有你能改，最可控，推荐）；b) 复用 AutoAccounting 社区规则（需写格式转换器，且引入对外依赖）；c) 先不做在线更新、保持纯手动热替换（P3 整段延后）。— 建议 a，初始规则包由本次开源调研成果直接生成
6. **主用版本（v1.1 新增）**：日常装 plus 版还是 offline 版？— 两者同签名可随时互切，建议日常 plus（规则更新省事），隐私敏感期或做行为审计时切 offline

> 已拍板事项：两大天花板事实已接受（§1.3）；审计 7 条结论已全部吸收进 v1.2（§4.3 / §4.4 / §8 / §9 / §10）。剩余待拍板：第 1、2、3、5、6 项，其中 2、3 需要看一眼手机。

---

## 附：与已生成骨架的差异清单

| 骨架现状 | 本方案修订 |
|---|---|
| `capture/CsvIo.kt`（csv 导入） | 改造为 `BillImporter`，前置 xlsx 解析；csv 支持保留为兼容路径 |
| 无 xlsx 读取能力 | 新增 `parse/xlsx/XlsxReader.kt`（系统 zip + 流式 XML，零新依赖） |
| `parser_rules.json` 仅通知规则 | 增加 `importProfiles`（两家表头映射、表头识别特征、状态过滤词），映射与枚举值直接吸收 double-entry-generator 的整理成果 |
| `tools/verify_parser_rules.py` 仅通知规则 | 扩展支持 xlsx 样例验证；测试样本吸收 seamless-bookkeeping 的 6 条真实通知 |
| 无网络代码 | 新增 `net/RuleUpdater.kt`（仅 plus flavor 编译入包）；`app/build.gradle.kts` 增加 productFlavors 配置 |
| `tools/verify_offline.sh` 单一验证 | 拆分为 offline 版专用 + 新增 `tools/verify_network_safety.py`（plus 版） |
| `TransactionDao` 仅判重丢弃 | v1.2：按 §4.3 实现先合并后判重（单号精确 → 宽松指纹 → 完整指纹 → 新增），回填只补空字段、永不覆盖用户修正 |
| 单测 3 个文件 | 增加 XlsxReader / BillImporter / RuleUpdater / 合并回填（构造重叠数据）测试 |
| 其余（加密/去重/分类/UI） | 不变 |
